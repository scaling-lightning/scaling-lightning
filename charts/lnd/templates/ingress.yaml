apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "lnd.fullname" . }}-traefix
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: Headers(`X-Node`, `{{ include "lnd.fullname" .}}`)
      kind: Rule
      services:
        - name: {{ include "lnd.fullname" . }}
          port: client